# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

FEATURES="ccache"
CCACHE_DIR="/var/cache/ccache"

#USE="X pgo -obex -systemd -aqua -css -a52 -cjk -3dfx -berkdb -clamav -avahi -consolekit -udisks -zeroconf -nls -doc -cdr -dvd -gnome-keyring -gstreamer -libav -coreaudio -ios -ipod -emacs -xemacs -openal -qt4 -qt5 -ldap -gtk -motif -emboss -altivec -smartcard -cups -ibm -nls -nas -neon -nntp -kde -gnome minimal opengl opencl infinality pulseaudio bluetooth"
#
USE="-wayland -debug -multilib -alsa -systemd -aqua -css -a52 -cjk -3dfx -berkdb -clamav -avahi -consolekit -udisks -zeroconf -nls -doc -cdr -dvd -gnome-keyring -gstreamer -libav -coreaudio -ios -ipod -emacs -xemacs -openal -qt4 -qt5 -ldap -gtk -motif -emboss -altivec -smartcard -ibm -nas -neon -nntp -kde -gnome -networkmanager -examples djvu mtp fontconfig dbus elogind minimal pulseaudio threads aio jit pgo dri dga opengl vaapi vdpau jpeg gif png exif bindist bluetooth X"

MAKEOPTS="-j5"

GENTOO_MIRRORS="http://mirror.eu.oneandone.net/linux/distributions/gentoo/gentoo/ http://mirrors.evowise.com/gentoo/ https://mirrors.evowise.com/gentoo/"

PYTHON_TARGETS="python2_7 python3_7"
PYTHON_SINGLE_TARGET="python3_7"

CPU_FLAGS_X86="aes avx avx2 f16c fma3 mmx mmxext pclmul popcnt sse sse2 sse3 sse4_1 sse4_2 ssse3"

GRUB_PLATFORMS="efi-64"

VIDEO_CARDS="intel i915"

INPUT_DEVICES="libinput"
